# AGENTS.md

このリポジトリでは、AIエージェントを使って個人開発を進めます。
AIエージェントは、以下のルールに従って作業してください。

## 基本方針

- GitHub Issueを作業単位とする
- Codexを実装、テスト、Git操作、PR作成の主担当とする
- Gemini、Antigravityなどの補助エージェントは、別解の検討、調査、UI案の比較に使う
- 複数のAIエージェントで同じブランチを同時に編集しない
- Issue対応は必ずしも直列に進めず、依存関係・変更箇所・作業競合を確認して安全に並列化できるIssueを判断する
- 並列で対応できるIssueがある場合は、対象Issue、並列化できる理由、競合しない前提をユーザーに伝えたうえで、Issueごとに別のCodexセッションとブランチ（必要に応じてworktree）を用意して対応する
- 依存関係があるIssue、同一ファイルや共有基盤を大きく変更するIssue、または競合リスクを安全に判断できないIssueは、直列で対応する
- Issueに書かれていない機能を勝手に追加しない
- 実装前にIssueの目的、実装範囲、受け入れ条件、スコープ外を確認する
- 技術スタック、提供形態、データ保存方式を勝手に変更しない
- 変更が必要な場合は、理由と影響範囲を説明してから進める
- MVPではシンプルさと公開可能性を優先する
- 過剰設計を避ける
- 今日中に動かせる粒度、公開できる粒度を優先する
- ユーザー体験に直接関係しない凝った実装を増やさない

## 作業開始前に確認すること

各Issueに着手する前に、以下を確認してください。

- このIssueの目的は何か
- 実装範囲はどこまでか
- スコープ外は何か
- 依存Issueは完了しているか
- 依存関係・変更対象・Open Pull Requestを確認し、他のIssueと安全に並列対応できるか
- 受け入れ条件は明確か
- 既存実装への影響はあるか
- どの検証コマンドを実行すべきか

不明点があっても、Issueと既存コードから安全に判断できる場合は、合理的な仮定を置いて作業を進めてください。
ただし、スコープや仕様が変わる判断が必要な場合は、実装前に確認してください。

## 設計Issueのルール

設計Issueでは、すぐに実装コードを書かないでください。

以下を整理してください。

- 解決したい課題
- 想定ユーザー
- MVP範囲
- 今回作らないこと
- 画面構成
- 入力項目、出力項目
- 技術スタック候補
- データ保存方式
- 公開方法
- 将来の拡張余地
- 実装Issueへの分解案

技術選定では、最初から特定の技術に固定せず、MVPの公開しやすさ、実装速度、保守性を比較してください。

## 実装Issueのルール

実装Issueでは、設計Issueや既存ドキュメントで決めた内容に従ってください。

- できるだけ小さな差分で実装する
- 1つのIssueで関係ないファイルを変更しすぎない
- 既存の構成、命名、スタイルに合わせる
- 型安全性を意識する
- 入力バリデーションを最低限入れる
- エラー時や空状態でアプリがクラッシュしないようにする
- UIはまず標準的で分かりやすいものにする
- localStorageなど端末内保存を使う場合は、読み込み失敗時にクラッシュしないようにする
- 外部API、ログイン、課金、広告SDK、解析SDKはIssueで明示されない限り追加しない

実装後は以下を報告してください。

- 実装概要
- 変更ファイル
- 動作確認結果
- 未対応事項
- 懸念点
- 次に進めるIssue
- worktreeを切って別環境で作業した場合は、作業ディレクトリの場所

## 実装依頼への対応フロー

ユーザーから実装を依頼された場合は、返答する前に原則として以下まで進めてください。

1. 対象Issueと関連ドキュメントを読む
2. 実装する
3. 関連する検証コマンドを実行する
4. 変更内容を確認する
5. コミットする
6. ブランチをpushする
7. GitHubのpull requestを作成する
8. PRの対応内容を学べる簡単な教材をリポジトリ内の `docs/.local/pr-learning.md` に作成する（教材はコミット不要とし、Gitの管理対象に含めない）
9. PRリンクと検証結果を返答する
10. PR作成後、レビュー指摘とCI結果を定期確認するため、Codexタスクの自動化を設定する
11. 未対応のレビュー指摘またはCI失敗がある間は、対応が明確なものを最小差分で修正し、検証、commit、push、対象PRへの対応コメントまで行う
12. 自動化が対象PRの`MERGED`を確認した場合は、マージ後の後片付けを実行し、自動化を停止する
13. 仕様変更、スコープ拡大、対応方針が曖昧な指摘は修正せず、判断が必要な点をユーザーへ報告する

PRは、明示がない限りDraftではなくOpen状態で作成してください。
教材には、対応した課題、主な変更、確認方法、学びどころを簡潔にまとめてください。
次の対応を開始する際は、既存の `docs/.local/pr-learning.md` を削除してから、その対応に対応する新しい教材を作成してください。
リポジトリの権限や認証の都合でpushやPR作成ができない場合は、その時点までの作業内容と未完了の手順を明確に報告してください。
Codexタスクの自動化を設定できない場合も、その理由と未完了の手順を明確に報告してください。

## レビュー対応

レビューコメントに対応する場合は、以下を守ってください。

- コメントの指摘内容を確認する
- ユーザーからレビューコメントの確認を依頼された場合、対応可否の承認は求めず、そのまま対応を進める
- 必要最小限の差分で修正する
- 修正後に関連する検証コマンドを実行する
- 変更をコミットしてpushする
- 対象PRへ対応内容をコメントする
- 完了報告では、各指摘の内容、対応可否の判断理由、実施した対応を明確に報告する

CodexのPRレビューはすべてのPRで使い、GitHub Actionsと人間の確認を補完してください。レビュー指摘は、対象Issueの目的、受け入れ条件、既存仕様への影響を確認してから対応します。

## マージ後の後片付け

ユーザーが「後片付け」「マージ後の整理」「ブランチ整理」と依頼した場合、またはPR作成後に設定したCodexタスクの自動化が対象PRの`MERGED`を確認した場合は、以下を行ってください。

1. 対象Pull RequestがGitHub上で`MERGED`であることを確認する。
2. `git status --short` を確認する。
   - 未コミット変更がある場合は、その内容を削除・ステージ・コミットしない。
   - `main` への切替や更新に影響する場合は、安全に続行できない理由を報告して止まる。
3. `origin/main` をfetchし、ローカル`main`を`origin/main`へfast-forwardで最新化する。
   - rebase、force push、resetは使わない。
4. 対象のローカル作業ブランチがマージ済みであることを確認してから削除する。
5. 対象のリモート作業ブランチが残っている場合だけ削除する。
   - 既に削除済みの場合は成功として扱う。
6. 最後に、現在のブランチが`main`であり、`main`と`origin/main`の差分が`0 0`であることを確認して報告する。

削除対象のブランチは、対象PRのhead branchだけに限定する。未マージブランチ、`main`、ユーザーの未コミット変更、教材などの未追跡ファイルは削除しない。

完了報告には、削除したローカル／リモートブランチ、`main`の最新コミット、未コミット変更の有無を簡潔に含める。
自動化で実行した場合は、後片付けの完了後にその自動化を停止または削除する。

## PRレビュー方針

GitHub上で動作するレビューエージェントを含め、PRレビューでは次を優先して確認してください。

- 対象Issueの受け入れ条件を満たしているか
- スコープ外の実装が混入していないか
- 既存仕様を壊す変更がないか
- 入力バリデーション、空状態、エラー処理に漏れがないか
- 保存データの互換性を壊していないか
- セキュリティ、認証、外部API、課金に影響がある場合は重点的に確認する
- 確実に問題となる指摘だけをコメントし、好みや任意改善は指摘しない

PR本文と紐づくIssueをレビュー根拠にし、Issueが確認できない場合は仕様上の推測による指摘を避けてください。

## レビュー時の確認観点

レビューでは以下を確認してください。

- Issueの受け入れ条件を満たしているか
- スコープ外実装が入っていないか
- ビルドエラーがないか
- テスト、lint、型チェックが通るか
- 入力エラーや空状態に対応しているか
- 保存処理や既存データの読み込みが壊れていないか
- UIが極端に崩れていないか
- スマホで主要操作ができるか
- 公開や審査提出に支障がないか
- 過剰実装になっていないか

## 禁止事項

- Issueにない機能追加
- 技術スタックの無断変更
- 不要な外部ライブラリ追加
- 広告SDKや解析SDKの無断追加
- ログイン機能の無断追加
- DBやサーバー処理の無断追加
- 既存仕様を壊す大規模リファクタリング
- READMEや設定ファイルの不要な書き換え
- 秘密情報、APIキー、個人情報のコミット

## 判断基準

迷った場合は、以下を優先してください。

1. 今日中に動くこと
2. 公開または審査提出できること
3. スコープが小さいこと
4. ユーザーが使えること
5. 後から拡張できること

完璧な設計より、公開可能なMVPを優先してください。
